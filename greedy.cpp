#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    int N, M;
    cin >> N >> M;
    
    vector<int> books(N);
    for (int i = 0; i < N; i++) {
        cin >> books[i];
    }
    
    sort(books.begin(), books.end()); // ????????????????????
    
    for (int i = 0; i < N - 2; i++) {
        int left = i , right = N - 1;
        
        while (left < right) {
            int total = books[i] + books[left] + books[right];
            
            if (total <= M) { 
                // ?????????????? ????????????????????????????
                cout << books[i] << endl;
                cout << books[left] << endl;
                cout << books[right] << endl;
                return 0;
            }
            if (total > M) right--; // ???????????? ?????????????????
            else left++; // ??????????? ?????????????????
        }
    }
    
    // ????????????????????
    cout << "0\n0\n0\n";
    return 0;
}

